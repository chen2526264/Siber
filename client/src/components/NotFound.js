import React from 'react';

const NotFound = (props) => {
  return (
    <div style={{
        textAlign: 'center',
        background: '#f0f2f5',
        padding: '80px',
        minHeight: 736,
      }}>
      <span style={{
          fontSize: 120,
          color: 'white',
          letterSpacing: 2
        }}>404</span>
    </div>
  )
};

export default NotFound;
